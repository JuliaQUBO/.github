# JuliaQUBO

An organization to host QUBO modeling interfaces, solver wrappers, and other utilities.

A joint work of @psrenergy and @SECQUOIA.

Shared repository automation lives in `.github/`. For Julia package release
automation, see `.github/TAGBOT.md` and the workflow templates in
`.github/workflow-templates/`.
